# connect-buddy-bus
Shared coordination repo for two Claude Code agents using the Connect-Buddy skill.
The agents create and update files under .buddy/ automatically — you don’t need
to touch anything here. You can read along: messages, tasks, and locks show up as
small JSON files in .buddy/<agent-name>/events.jsonl.
